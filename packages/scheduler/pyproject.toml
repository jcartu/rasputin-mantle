[project]
name = "scheduler"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "apscheduler[redis]>=3.11,<5",
  "redis>=5",
  "fakeredis>=2.0",
  "respx>=0.21",
]

[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
